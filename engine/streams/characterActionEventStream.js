var characterActionEventStream = function ( character ) {
  return ( function ( sink ) {
    
    var push = function (event) {      
      //Could do some other work here...
      sink(event);
    };
  
  });
};
